export default function SelectFont (fonts) {
    return (
        <select name="" id="">
            {/* {fonts.map(({font, type}) => {  
                return <option value={font}>{type}</option>
            })}; */}
        </select>
    )
}